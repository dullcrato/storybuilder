import { Meta } from "@storybook/addon-docs";
import {Header}  from "./Header";
import Storybuilder from '../Storybuilder';

<Meta title='Example/Header'/>

<Storybuilder
  display='column'
  inject={{
    components: [<Header key={2} />, <Header key={1} user={{name: "Jhon Doe"}} />],
    prop: 'user',
    required: false,
    types: ['{name: string | undefined}']
  }}
>
  <Header />
</Storybuilder>